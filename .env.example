# Copy to .env and fill in. NEVER commit the real .env.
# Production: keep the filled file only at /opt/bedem/.env on the server and in
# a password manager. Generate every secret fresh per environment.

# PostgreSQL
POSTGRES_DB=bedem
POSTGRES_USER=bedem
# openssl rand -base64 24 | tr '+/' '-_' | tr -d '='
POSTGRES_PASSWORD=

# pgAdmin (development only; not deployed in production)
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=

# JWT
# openssl rand -base64 48
JWT_SECRET=
JWT_ISSUER=BedemApi
JWT_AUDIENCE=BedemApp

# Public origin of the deployed site, e.g. https://bedem.rs
# Used by Caddy for the TLS certificate and by the frontend build.
SITE_DOMAIN=
